FROM dhi.io/node:lts-alpine AS runner
WORKDIR /app
ARG HTTP_PROXY HTTPS_PROXY http_proxy https_proxy ALL_PROXY all_proxy
ENV http_proxy=${HTTP_PROXY} https_proxy=${HTTPS_PROXY} HTTP_PROXY=${HTTP_PROXY} HTTPS_PROXY=${HTTPS_PROXY} ALL_PROXY=${ALL_PROXY}
COPY --from=specodec-runtime-ts . /specodec-ts/
COPY --from=run package.json tsconfig.json run.ts ./
COPY --from=vectors . /app/vectors/
RUN mkdir -p /app/output/scalars
ENV VEC_DIR=/app/vectors OUT_DIR=/app/output
RUN cd /specodec-ts && npm install && cd /app && npm install && npx tsx run.ts